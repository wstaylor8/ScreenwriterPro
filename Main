// ScreenWritingProApp.swift
import SwiftUI

@main
struct ScreenWritingProApp: App {
    @StateObject private var viewModel = ScriptViewModel()
    
    var body: some Scene {
        WindowGroup {
            ContentView()
                .environmentObject(viewModel)
        }
    }
}
